<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="framework.mybatis.mapper.UserMapper">
	<select id="selectByName" resultType="framework.mybatis.modal.User">
		select * from userinfo where UserName = #{userName}
	</select>
</mapper>